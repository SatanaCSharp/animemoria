import { AuthClientService } from 'shared/client-services/auth.client-service';
import { RegistryClientService } from 'shared/client-services/registry.client-service';

export const clientServices = [RegistryClientService, AuthClientService];
