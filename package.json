{
  "name": "animemoria",
  "version": "1.0.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.6.3"
  }
}
