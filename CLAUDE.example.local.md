# Local overrides — never commit this file

## Database URLs

```
USERS_DB_URL=your_users_db_url
AUTH_DB_URL=your_auth_users_db_url
```

## Sandbox endpoints

<!-- Override registry URL if pointing at a remote dev cluster -->
<!-- REGISTRY_URL=http://... -->

## Personal preferences

<!-- Anything you'd otherwise re-type each session goes here -->
